#!/bin/sh
# postrm script for kvalobs
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postrm> `remove'
#        * <postrm> `purge'
#        * <old-postrm> `upgrade' <new-version>
#        * <new-postrm> `failed-upgrade' <old-version>
#        * <new-postrm> `abort-install'
#        * <new-postrm> `abort-install' <old-version>
#        * <new-postrm> `abort-upgrade' <old-version>
#        * <disappearer's-postrm> `disappear' <overwriter>
#          <overwriter-version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package

. /usr/share/debconf/confmodule


#BINDIR, BIN and DIR must be in sync with the respectives variables in postrm.
BINDIR="/usr/bin"
BIN="norcom2kv"

KVBINDIR=""
KVBIN=""

ETC_FILES=" "

DIRS="/usr/share/norcom2kv 
      /var/lib/norcom2kv
      /var/run/kvalobs
      /var/lib/norcom2kv/
      /var/lib/norcom2kv/tmp
	  /var/lib/norcom2kv/data2kv"      


case "$1" in
    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
    ;;

  purge)
    for i in $BIN; do
        dpkg-statoverride --remove $BINDIR/$i
     done

    for i in $KVBIN; do
        dpkg-statoverride --remove $KVBINDIR/$i
     done

     for i in $DIRS; do
        dpkg-statoverride --remove $i
     done

     
     for i in $ETC_FILES; do
	 	dpkg-statoverride --remove /etc/kvalobs/$i
     done
     
     dpkg-statoverride --remove $SQLLITE_DB
     
     #Delete my debconf entries in the database.
     db_purge || true
     ;;
   
    *)
     echo "postrm called with unknown argument \`$1'" >&2
     exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0


